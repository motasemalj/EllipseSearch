# ===========================================
# RPA Browser Automation Configuration
# ===========================================
# Copy this file to .env and fill in your values

# Chrome DevTools Protocol connection
CDP_URL=http://localhost:9222

# Timing Configuration (seconds between prompts)
MIN_DELAY=5
MAX_DELAY=10

# Output file path (backup - results also sent to platform)
OUTPUT_JSON=./rpa_results.json

# Logging
LOG_FILE=./rpa_log.txt
SCREENSHOT_DIR=./screenshots

# ===========================================
# Platform Integration (REQUIRED)
# ===========================================
# The RPA fetches prompts FROM and sends results TO the platform

# Base URL of your EllipseSearch deployment
PLATFORM_URL=http://localhost:3000

# Webhook secret - MUST MATCH the RPA_WEBHOOK_SECRET in your platform's .env
RPA_WEBHOOK_SECRET=your-secure-random-secret-here

# Language and region for analysis (defaults)
LANGUAGE=en
REGION=global

# ===========================================
# Usage Examples
# ===========================================
# 
# 1. Fetch prompts from platform and run:
#    python main.py --brand-id YOUR_BRAND_UUID --engine chatgpt
#
# 2. Run specific prompt set:
#    python main.py --brand-id YOUR_BRAND_UUID --prompt-set-id YOUR_SET_UUID --engine all
#
# 3. Single prompt test:
#    python main.py --prompt "Best CRM software" --brand-id YOUR_BRAND_UUID --engine chatgpt
#
# Get your brand_id from the URL when viewing a brand in the dashboard:
# https://your-platform.com/brands/[THIS-IS-YOUR-BRAND-ID]/prompts

